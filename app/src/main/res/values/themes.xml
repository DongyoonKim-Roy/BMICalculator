<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BMICalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/navy</item>
        <item name="colorPrimaryVariant">@color/navy</item>
        <item name="android:statusBarColor">@color/navy</item>
        <item name="android:buttonStyle">@style/ButtonStyle</item>
    </style>

    <style name="Theme.BMICalculator" parent="Base.Theme.BMICalculator" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/darkBaige</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/horselogo</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.BMICalculator</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/navy</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>